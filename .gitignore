# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

train/

*.pth
*.pt
*.db

/dataset/

debug/

debug_output/

debug_yolo_checks/


*debug*
multirun/
site/
# Local debug frames dumped at repo root
frame_*.npy
outputs/
outputs

# W&B local run-metadata cache (still uploaded to the cloud project; this is
# just the local mirror wandb.init() writes even when online).
wandb/

# Sweep result databases: small enough to keep deliberately-committed ones
# (see git history), but new sweep runs shouldn't get auto-added by accident.
sweeps/*.db
depth_image.py
inspect_crop.py
scratch/DIFF_WALKTHROUGH.md

scratch
benchmark_runs/
sweeps/*_frames/
log_*.txt